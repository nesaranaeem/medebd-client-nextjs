"use client"; // This is a client component 👈🏽
import Statistics from "./components/statistics/Statistics";

const page = () => {
  return (
    <div>
      <Statistics />
    </div>
  );
};

export default page;
